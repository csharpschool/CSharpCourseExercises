@page "/exercise-04-02"
@using CSharpExercises.Classes.Chapter_04

<h3>Calculator</h3>
<input @bind="@value1" placeholder="Enter a value" />
<input @bind="@value2" placeholder="Enter a value" />
<br />
<button class="btn-sm btn-light" @onclick="Add">+</button>
<button class="btn-sm btn-light" @onclick="Subtract">-</button>
<button class="btn-sm btn-light" @onclick="Divide">/</button>
<button class="btn-sm btn-light" @onclick="Multiply">x</button>

<h4>@result</h4>

@code {
    string result = string.Empty;
    double value1 = default, value2 = default;

    void Add()
    {
        var values = new Values(value1, value2);
        var total = values.Add();
        result = total.Output();
    }

    void Subtract()
    {
        var values = new Values(value1, value2);
        result = values.Subtract().Output();
    }

    void Divide()
    {
        result = new Values(value1, value2).Divide().Output();
    }

    void Multiply()
    {
        result = new Values(value1, value2).Multiply().Output();
    }
}
